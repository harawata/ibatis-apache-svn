The iBATIS JAR files are locally generated (unreleased).  They include the nested iterate fixes that
are required for the tests to run properly.

The iBATIS files should be replaced once 2.2.0 is released.
